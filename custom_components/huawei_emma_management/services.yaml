# Public actions are registered dynamically under the huawei_emma domain so external
# controllers do not depend on this integration's internal config-entry domain. Their
# UI schemas are installed with async_set_service_schema in __init__.py.
